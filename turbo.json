{
  "$schema": "https://turborepo.org/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["src/*", "package.json"],
      "outputs": ["dist/*"]
    },
    "//#fmt": {
      "cache": false
    }
  }
}
